import {Compare} from './compare';

export class PageCompare {
  content: Compare[];
  totalPages: number;
  totalElements: number;
  last: boolean;
  size: number;
  first: boolean;
  sort: string;
  numberOfElements: number;
}
